/**
 * Attribution for cached NSW Spatial Services feature data (address points,
 * properties, proways, waypoints) rendered on the maps. NSW SS data is licensed
 * CC BY 4.0, which requires this attribution plus a data-currency date — the date
 * is appended at runtime by MapView via `MapProfile.showDataCurrency`.
 *
 * NSW Spatial Services sits within the Department of Customer Service, which is the
 * copyright holder named in the licence terms.
 */
export const NSW_SS_DATA_ATTRIBUTION =
	"Data © <a href='https://www.spatial.nsw.gov.au' target='_blank' rel='noopener'>Department of Customer Service (NSW Spatial Services)</a>, CC BY 4.0";

/**
 * Attribution for NSW Spatial Services *basemap tiles* (the SIX street/aerial map
 * services), distinct from the feature-data attribution above. Same DCS wording so
 * the two read consistently in the attribution control.
 */
export const NSW_SS_BASEMAP_ATTRIBUTION =
	"© <a href='https://www.spatial.nsw.gov.au' target='_blank' rel='noopener'>Department of Customer Service (NSW Spatial Services)</a>";
